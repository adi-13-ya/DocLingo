# PlantUML Generation Prompt for GPT

## Single Paragraph Prompt:

Generate PlantUML code for four separate diagrams (each as a separate @startuml block) for a multilingual document question-answering system called DocLingo: (1) **Query Intent Classification & Routing Diagram** - showing how user queries flow through an IntentClassifier that uses keyword matching and regex patterns to classify queries into intents (Metadata, Aggregate, Analytical, Summarization, Interpretation, Comparison, Causation, etc.), then routes them through a 3-tier system where TIER 1 (Programmatic engines like MetadataEngine, AggregateEngine, AnalyticalEngine) handles queries without LLM, TIER 2 (Specialized LLM engines like SummarizationEngine, InterpretationEngine, ComparisonEngine) handles category-specific queries with specialized prompting, and TIER 3 (ContentEngine) serves as fallback for general content queries, with decision diamonds showing whether each tier can handle the query or falls through to the next tier; (2) **Retrieval-Augmented Generation (RAG) Pipeline Diagram** - showing the complete flow from document loading and text extraction, through chunking (500 chars with 50 char overlap), embedding generation using OpenAI text-embedding-3-small model (1536 dimensions), FAISS indexing (IndexFlatL2) that stores vectors with metadata (page numbers, chunk indices), query embedding generation, semantic search in FAISS that returns top-k chunks with similarity scores (L2 distance converted to similarity), context formatting with page citations, and finally answer generation using GPT-4o-mini with document-grounded prompting that strictly uses only the retrieved context chunks; (3) **Explainability & Confidence Alignment Flow** - showing how after answer generation, the system generates explanations using an Explanation Generator that creates human-readable rationale (in query language) explaining why specific document sections were used, while simultaneously computing base confidence scores based on number of chunks retrieved and similarity scores, then passing the base confidence through a Confidence Calibrator that adjusts confidence levels (High/Medium/Low) based on historical user feedback patterns (analyzing feedback by intent, engine, and confidence level to identify if high confidence queries underperform or low confidence queries overperform), with the final calibrated confidence and explanation text being combined into the response; (4) **Feedback-Driven Self-Improvement Loop** - showing a circular flow where user feedback (rating 1-10) is collected via FeedbackLogger and stored in feedback_log.json with rich metadata (query, answer, intent, engine, translation strategy, retrieval depth, confidence, execution time), then FeedbackAnalyzer aggregates statistics by intent, engine, translation strategy, and confidence level to identify best-performing strategies and optimal retrieval depths, followed by StrategyOptimizer using this analysis to adjust parameters (retrieval depth optimization from 2-10 chunks based on feedback correlation, preferred translation strategy selection), and ConfidenceCalibrator adjusting confidence thresholds, with these optimized parameters then being fed back into the Adaptive Decision Engine and used in future queries, creating a continuous improvement cycle where the system learns from feedback to optimize retrieval depth, translation strategies, and confidence calibration over time. Use black and white color scheme only, ensure all text has correct spelling, use appropriate PlantUML shapes (rectangles for processes, diamonds for decisions, cylinders for data stores, arrows for flow), include clear labels and notes, and make each diagram self-contained and professional.


